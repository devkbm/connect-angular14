export interface Term {
  termId: string | null;
  system: string;
  term: string;
  termEng: string;
  columnName: string;
  domainId: string | null;
  domainName: string;
  description: string;
  comment: string;
}
